package com.example.volumereplacer

import android.app.Application

/**
 * Application-level entry point.
 * Initialise app-wide singletons (DataStore, repositories) here later.
 */
class VolumeReplacerApp : Application() {

    override fun onCreate() {
        super.onCreate()
        // TODO: initialise DataStore, logging, DI graph
    }
}

